{{ 'section-comment.css' | asset_url | stylesheet_tag }}
{{ 'article-comments.css' | asset_url | stylesheet_tag }}
{{ 'article-comments-form.css' | asset_url | stylesheet_tag }}

<section {% render 'section-attr' %}>
  <div class='xo-section-comment'>
    {%- if blog.comments_enabled? -%}
      {% render 'article-comments' %}
      {% render 'article-comments-form' %}
    {%- endif -%}
  </div>
</section>

{% schema %}
{
  "name": "t:sections.comment.name",
  "settings": [
    {
      "type": "header",
      "content": "t:sections.all.heading.content"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.color_scheme.label"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "t:sections.all.bg_color.label"
    },
    {
      "type": "color_background",
      "id": "background_gradient",
      "label": "t:sections.all.bg_gradient.label"
    },
    {
      "type": "number",
      "id": "padding_top_desktop",
      "label": "t:sections.all.padding_top_desktop.label",
      "default": 50
    },
    {
      "type": "number",
      "id": "padding_bottom_desktop",
      "label": "t:sections.all.padding_bottom_desktop.label",
      "default": 50
    },
    {
      "type": "number",
      "id": "padding_top_mobile",
      "label": "t:sections.all.padding_top_mobile.label",
      "default": 50
    },
    {
      "type": "number",
      "id": "padding_bottom_mobile",
      "label": "t:sections.all.padding_bottom_mobile.label",
      "default": 50
    }
  ]
}
{% endschema %}
